# Generated targets

W01 and C01 are now present as real generated **candidates**, not approved construction targets. See [review, provenance and blockers](../reviews/20260912-W01-C01/README.md). W01 still violates the spatial/progression contract; C01 still needs age, hair and shared-style refinement. File validity is not design approval.

Use the actual image provider, then import PNGs through `Tools/VisualTargets/atlas.py import-target`. Files begin as candidates. Approval requires a real reviewer decision. Historical captures and SVG layout drafts must not be renamed into generated targets.
