
;不動産
@ITEM_SHOP_ESTATE
#DIM DYNAMIC SELECT_NUM

CLEARLINE LINECOUNT
CALL SHOW_RESOURCE_SHOP_LOAN
CALL CAN_LOAN_MONEY_CALC
PRINTL 　　　《 Buildings・Additions 》
CALL ESTATE_LIST
DRAWLINE
CALL ITEM_SHOP_ESTATE_DETAIL(SELECT_NUM)
CALL PRINTL_ALIGN
CALL ESTATE_JUDGE(SELECT_NUM)
DRAWLINE
CALL RIGHT_RETURN_BUTTON
BINPUT
PRINTL
SELECTCASE RESULT
CASE 0
	;購入処理→SELECT_NUMで変化
	CALL TOWN_ESTATE_BUY(SELECT_NUM)
CASE 99
	RETURN
CASE 10 TO 10 +ESTATE_LIST_NUM
	SELECT_NUM = RESULT
ENDSELECT
RESTART



;購入処理
;ARG→INPUT
@TOWN_ESTATE_BUY(ARG)
#DIM EXTENTION_NUM
#DIM COWSHED_LIMIT
#DIMS BUILDING

SELECTCASE ARG
CASE 11
	;増築する牛舎を先に選択
	PRINTL Select a cowshed to expand. Each expansion increases capacity by 10.
	
	;FLAG:第1牛舎増築段階→500
	;FLAG:第1牛舎家畜頭数→502
	;-10
	FLAG_REF = 490
	FLAG_REF:1 = 492
	FOR LOCAL,1,11
		COWSHED_LIMIT = FLAG:(FLAG_REF +LOCAL *10) *10
		SIF !COWSHED_LIMIT
			BREAK
		IF COWSHED_LIMIT >= 100
			CALL PRINTL_WORD_COLOR,@"%ITEMS_ENG(COWSHED_NAME:LOCAL)% already at max size","灰"
		ELSE
			PRINTFORM [{LOCAL,2}] %ITEMS_ENG(COWSHED_NAME:LOCAL),24%：
			PRINTFORML Occupancy {FLAG:(FLAG_REF:1 +LOCAL *10),2}／{COWSHED_LIMIT,2} （Max 100）
		ENDIF
	NEXT
	DRAWLINE
	PRINTL [99] Done
	BINPUT
	PRINTL
	SIF RESULT == 99
		RETURN
	EXTENTION_NUM = RESULT
	CALL TOWN_ESTATE_BUY_MESSAGE(ARG,"牛舎増築")
	CALL ASK_BUILD
	SIF !RESULT
		CALL BUILD_COMMON(ARG,EXTENTION_NUM,"牛舎増築")
	
;通常建築処理は同じ
;発注ミス防止に先に建築物名を呼び出し
CASE 10,12 TO 10 +ESTATE_LIST_NUM
	CALL TOWN_ESTATE_BUILDING_REF(ARG)
	BUILDING = %RESULTS%
	CALL TOWN_ESTATE_BUY_MESSAGE(ARG,BUILDING)
	CALL ASK_BUILD
	SIF !RESULT
		CALL BUILD_COMMON(ARG,0,BUILDING)
ENDSELECT



;建築依頼
@ASK_BUILD

PRINTL
PRINTL Request Construction?
PRINTLC [ 0] Construct
PRINTL [ 1] Cancel
INPUT
IF !RESULT
	RETURN 0
ELSE
	RETURN 1
ENDIF


