
;奴隷候補の陰毛
@RANDOM_SLAVE_UNDERHAIR

IF CHILD_HEIGHT_CHECK(CSTR:身長)
	;FLAG:子供候補割合／陰毛／パイパン→3230
	FLAG_REF = 3230
ELSE
	;FLAG:候補割合／陰毛／パイパン→3140
	FLAG_REF = 3140
ENDIF
STR_REF = 6850
LAST_SHUFFLE_S = %CSTR:陰毛%
CALL RANDOM_BOX_LOTTERY(9)

;種族補正作るかもしれない
IF SHUFFLE_COUNT < 10 && LAST_SHUFFLE_S == RESULTS
	SHUFFLE_COUNT ++
	RESTART
ENDIF
SHUFFLE_COUNT = 0
CSTR:陰毛 = %RESULTS%

