
;奉仕判定
;メッセージ部分が本体みたいなとこある
@SERVICE_JUDGE

IF !BASE:体力
	PRINTFORMW %CALLNAME%'s stamina has run out, so she is unable to serve...
	RETURN
	
ELSEIF CFLAG:崩壊
	PRINTFORMW %CALLNAME% whose mind is shattered cannot move properly and is unable to serve...
	RETURN
	
ELSEIF CFLAG:洗脳
	PRINTFORML Upon being instructed to serve, the brainwashed %CALLNAME% obediently began, her voice soft and compliant...
	
ELSEIF ＃陥落状態／隷属＃
	PRINTFORML When instructed to serve, %CALLNAME% began the act with genuine joy...
	
ELSEIF ＃陥落状態／恋慕以上＃ || CFLAG:乳牛
	PRINTFORML When instructed to serve, %CALLNAME% began the act with a smiling face...
	
ELSEIF ＃陥落状態／陥落以上＃
	;処女で騎乗位させる場合ちょっと特殊
	IF SELECTCOM == 34 && TALENT:処女
		PRINTFORML When instructed to take the riding position, %CALLNAME% seemed reluctant to lose her virginity in this way...
		IF MARK:淫紋
			CALL PRINTL_WORD_COLOR("However, she could not resist the desire generated by the mark and began the act...","桃")
		ELSE
			PRINTW
			RETURN
		ENDIF
	ELSE
		PRINTFORML When instructed to serve, %CALLNAME% began the act hesitantly...
	ENDIF
	
ELSEIF MARK:淫紋
	CALL PRINTL_WORD_COLOR,@"%CALLNAME% resisted serving, but could not resist the desire created by the mark and began the act...","桃"
	
ELSEIF PALAM:淫欲 >= 10000
	IF SELECTCOM == 34 && TALENT:処女
		PRINTFORMW When instructed to take the riding position, %CALLNAME% seemed reluctant to lose her virginity in this way...
		RETURN
	ELSE
		PRINTFORML %CALLNAME% resisted at first, but the desire in her pleasure-trained body was too much, and she began the act...
	ENDIF
	
ELSEIF ABL:奉仕精神 >= 5 && BASE:抵抗心 < MAXBASE:抵抗心 /2
	IF SELECTCOM == 34 && TALENT:処女
		PRINTFORMW When instructed to take the riding position, %CALLNAME% seemed reluctant to lose her virginity in this way...
		RETURN
	ELSE
		PRINTFORML Driven by her own sense of devotion, %CALLNAME% gave in to you and began the act...
	ENDIF
	
ELSE
	PRINTFORMW %CALLNAME% was instructed to serve but is reluctant...
	RETURN
ENDIF

;RETURN1したらOK、しかメッセージ以外の処理する場所がないのでここでやっちゃう
RETURN 1




