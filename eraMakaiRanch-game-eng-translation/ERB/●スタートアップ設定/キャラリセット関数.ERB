
;キャラリセット関数
;ARG→リセットして追加するキャラ番号
@NEWGAME_CHARA_RESET(ARG)
#DIM SWAP_TARGET

;追加→入れ替え→消去→初期フラグ入れ
ADDCHARA ARG
SWAP_TARGET = CHARANUM -1
SWAPCHARA TARGET,SWAP_TARGET
DELCHARA SWAP_TARGET

;あなたは後でやるので飛ばす
SIF TARGET
	CALL CSV_CHARA_ADD_FIX(1)





