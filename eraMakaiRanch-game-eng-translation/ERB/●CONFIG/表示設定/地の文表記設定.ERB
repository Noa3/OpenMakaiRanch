
;ちんぽと触手ちんぽも入れる
;地の文の表記設定
@SENTENCE_CONFIG

CLEARLINE LINECOUNT
PRINTL Change how the following parts are generally referred to
DRAWLINE
STR_REF = 6640
STR_MAX = 50
FOR LOCAL,0,10
	SIF STR:(STR_REF +LOCAL) == ""
		BREAK
	PRINTFORML [{LOCAL,2}] %REFPARTS_ENG(STR:(STR_REF +LOCAL)),17%：%SAVESTR:LOCAL%
NEXT
CALL PRINTL_ALIGN(-10)
CALL DEFAULT_VOID_RETURN_BUTTON(1)
INPUT
PRINTL
SELECTCASE RESULT
CASE 0
	PRINTL Change the term of reference for ＜Breasts＞
	PRINTL ※This will be used in many contexts, so make it generalizable 
	
	;STR参照に変更
	STR_REF = 2100
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／乳房 = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter a term of reference for ＜Breasts＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／乳房 = %RESULTS%
	ENDSELECT
	
CASE 1
	PRINTL Change the term of reference for ＜Milk＞
	STR_REF = 2150
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／母乳 = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter a term of reference for ＜Milk＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／母乳 = %RESULTS%
	ENDSELECT
	
CASE 2
	PRINTL Change the term of reference for ＜Mana-Infused Milk＞
	
	;魔力+（入り+）参照文字列なので少し変化
	STR_REF = 2200
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] Mana %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF +STR_MAX,3}] Mana-Infused %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 49
		SAVESTR:表記／魔力母乳 = Mana %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 50 TO 100
		SAVESTR:表記／魔力母乳 = Mana-Infused %NAMEPARTS_EN(STR:(STR_REF +RESULT -STR_MAX))%
	CASE 901
		PRINTL Enter a term of reference for ＜Mana-Infused Milk＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／魔力母乳 = %RESULTS%
	ENDSELECT
	
CASE 3
	PRINTL Change the term of reference for ＜Pussy＞
	STR_REF = 2250
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／膣 = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter a term of reference for ＜Pussy＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／膣 = %RESULTS%
	ENDSELECT
	
CASE 4
	PRINTL Change the term of reference for ＜Ass＞
	STR_REF = 2300
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／アナル = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter a term of reference for ＜Ass＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／アナル = %RESULTS%
	ENDSELECT
	
CASE 5
	PRINTL Change the term of reference for ＜Penis＞
	STR_REF = 2350
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／ペニス = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter the term of reference for ＜Penis＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／ペニス = %RESULTS%
	ENDSELECT
	
CASE 6
	PRINTL Change the term of reference for ＜Tentacle Penis＞
	
	;触手～／～触手（ペニス）
	STR_REF = 2350
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)% Tentacle
	NEXT
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF +STR_MAX,3}] Tentacle %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 49
		SAVESTR:表記／ペニス触手 = %NAMEPARTS_EN(STR:(STR_REF +RESULT))% Tentacle
	CASE 50 TO 100
		SAVESTR:表記／ペニス触手 = Tentacle %NAMEPARTS_EN(STR:(STR_REF +RESULT -STR_MAX))%
	CASE 901
		PRINTL Enter a term of reference for ＜Tentacle Penis＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／ペニス触手 = %RESULTS%
	ENDSELECT
	
CASE 7
	PRINTL Change the term of reference for ＜Clit＞
	STR_REF = 2400
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／陰核 = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter a term of reference for ＜Clit＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／陰核 = %RESULTS%
	ENDSELECT
	
CASE 8
	PRINTL Change the term of reference for ＜Semen＞
	STR_REF = 2450
	FOR LOCAL,STR_REF,STR_REF +STR_MAX
		SIF STR:LOCAL == ""
			CONTINUE
		PRINTFORMLC [{LOCAL -STR_REF,3}] %NAMEPARTS_EN(STR:LOCAL)%
	NEXT
	CALL INPUTBUTTON
	BINPUT
	SELECTCASE RESULT
	CASE 0 TO 99
		SAVESTR:表記／精液 = %NAMEPARTS_EN(STR:(STR_REF +RESULT))%
	CASE 901
		PRINTL Enter a term of reference for ＜Semen＞
		INPUTS
		SIF RESULTS != ""
			SAVESTR:表記／精液 = %RESULTS%
	ENDSELECT
	
CASE 98
	PRINTL Restore the default terms of reference?
	CALL ASK_YN
	SIF !RESULT
		CALL DEFAULT_SENTENCE_CONFIG
CASE 99
	RETURN
ENDSELECT
RESTART


