# Kanban

Updated 2026-09-07. Status is evidence-based; DONE applies only to the named scope. Long-term 3D remake remains incomplete. See `3D_REMAKE_PLAN.md` for execution details and `KNOWN_ISSUES.md` for defect evidence.

## Done — baseline scope

- **AUDIT-001** Repository/docs/code inventory and composition map. Evidence: CURRENT_PROJECT_STATE and audit/simulation-map.
- **AUDIT-002** Main build and disposable-profile smoke; malformed scene headers repaired, navigation assertions corrected. Smoke: 868 assertions, PASS. Existing compile warning recorded separately.
- **TOOLS-001** Shared stable-4.7 .NET launcher; real editor/runtime MCP adapter, screenshot verification and safety regressions. Preserve loopback/development-only limits.
- **DATA-001** All 11 original Chara CSVs neutrally audited, mapping uncertainty and age risks documented. 136 field citations and 12 input hashes rechecked. This is not a full dialogue/image identity audit or runtime adult guard.
- **TOOLS-004** Character audit evidence verifier hardened: explicit checks survive Python optimization, validate citation identity/paths/lines, and distinguish original-source verification from stale code snapshots. Original source evidence rechecked; full snapshot remains stale after SAVE-001/CORE-002. This does not complete DATA-002. See WORK_LOG for fresh verification.
- **DATA-002** Fail-closed adult eligibility gates implemented in code (not assets, not numeric relabeling). `AdultEligibilityGate` enforces Unknown/Minor/Ambiguous denial at definition import, generation, mature-action dispatch, player mutation, player picker, and save-load. 12 dedicated gate smoke assertions added. Full isolated smoke **998 assertions PASS**. See WORK_LOG for scope and evidence.
- **CORE-001** Current state, additive 3D migration plan and resumable repository documents established.
- **SAVE-001** Flag roundtrips and explicit-null migration repaired. 81 root regression assertions; full isolated smoke 949 PASS. Invalid-load fixtures preserve live references and original file contents. No schema bump.
- **WORLD-001 (greybox slice)** Opt-in ranch greybox against the ART-001 floor plan. `src/World/` holds the deterministic, Node-free math + guard layer (`WorldMovementMath`, `WorldCameraMath`, `WorldInputGate`, `WorldInteractionGuard`) and the node layer (`ThirdPersonPlayerController`, `WorldCameraRig`, `WorldStation`, `RanchGreyboxController`, `WorldInputBootstrap`). The `IWorldCommandDispatcher` boundary routes every station interaction to `GameRoot` (no second reward calculator). `scenes/dev/RanchGreybox.tscn` is opt-in (not the boot scene) with ground/walls/interior/station/player/camera/prompt/management button. 40+ dedicated world smoke assertions cover diagonal-vs-straight speed, camera clamps, input ownership, double-activation, dispatcher dispatch, and the scene node contract. Full isolated smoke **1038 assertions PASS** on Godot 4.7.2 mono.
- **WORLD-002 (shared-simulation proof)** A `WorldStation` bound to the production `GameRootCommandDispatcher` (live `GameRoot.Instance`) mutates the **same** `ScheduleService` state the management UI reads, through the real `TryAssignJob` boundary. Exactly one settlement reward authority (the world path does not pay work, advance time, or create a second economy). Stale `StateGeneration` commands are rejected. 6 dedicated smoke assertions. Full isolated smoke **1044 assertions PASS** on Godot 4.7.2 mono.
- **CHAR-001 (gate-safe stand-in avatars)** `src/Character/` — `CharacterVisualProfile` (presentation-only Resource, stable `DefinitionId`, fail-closed `AdultEligibility` + `Provenance`, `IsDebugStandIn` flag, **no gameplay numbers**), `CharacterAvatarFactory` (pure Node-free, `CreateProfile` + fail-closed `CanUseRealAvatar` gate: Unknown/Minor/Ambiguous deny, only ConfirmedAdult permits — but CHAR-001 ships stand-ins only, the gate does not unlock real models yet), `CharacterAvatar3D` (honest debug stand-in: capsule body + sphere head, `Rebuild()` idempotent, `MaterialOverride` tint from profile). 19 dedicated smoke assertions (gate, profile binding, presentation/simulation separation, node geometry, rebuild idempotency). Full isolated smoke **1063 assertions PASS** on Godot 4.7.2 mono.
- **EVENT-001 (dialogue staging + safe panel coordinator)** `DialogueStaging` (pure presentation, state-free — no bond/morale/stockpile/completed fields, structurally cannot duplicate event state; deterministic framing) + `WorldPanelCoordinator` (open a management panel from the world, suspend world input, close and resume world input safely — gate never left UI-owned; single-panel rule + unknown-panel rejection). 26 dedicated smoke assertions. The command boundary (Mentorship + BondEvent, duplicate protection, exact rewards) was already proven by WORLD-002 + GameCommandTests. Full isolated smoke **1088 assertions PASS** on Godot 4.7.2 mono.
- **SAVE-002 (full 3D-world save/load round-trip)** `TestSaveLoadRoundTrip`: job assignment + mentorship (social action) + a full day transition through the 3D world path (station → production dispatcher → GameRoot) persist to the disposable smoke slot (99), survive a fresh start, and load back intact (world assignment, bond, morale, day counter, morning phase — no data loss). A stale (pre-load) `StateGeneration` is rejected via the world path; the disposable slot is cleaned up. 17 dedicated smoke assertions. Fresh starts expected (D-011); no old-save migration. Full isolated smoke **1105 assertions PASS** on Godot 4.7.2 mono.
- **WORLD-003a (day-phase lighting + logical roster placement)** `DaylightMath` (pure, Node-free: shared `DayPhase` → resolved `DaylightState`, deterministic hand-tuned table — no second clock) + `DaylightRig` (applies the state to the sun + environment; reads the phase from `GameRoot`) + `RosterPlacementMath` (pure: job category → in-bounds greybox anchor, bounded side-by-side spread, unknown → rest area) + `RosterRig` (places CHAR-001 stand-ins for the live roster; idempotent; moves presentation only — the shared schedule is untouched, no second work economy). 25 dedicated smoke assertions. Full isolated smoke **1140 assertions PASS** on Godot 4.7.2 mono.
- **WORLD-003b (greybox = live view of the shared simulation)** `RanchGreyboxController.WireLiveWorld`/`RefreshLiveWorld`: on entering the tree the greybox binds its daylight + roster rigs to the shared `GameRoot` (lighting reflects the current phase, CHAR-001 stand-ins placed for the live roster — no manual wiring). Scene carries `DaylightRig` + `RosterRig`. `TestGreyboxSceneIsLive` proves the composition: scene instantiates into a live view (lighting == shared phase, one avatar per character, in-bounds, player/station intact, re-derives after a phase change). 14 dedicated smoke assertions. Full isolated smoke **1153 assertions PASS** on Godot 4.7.2 mono.
- **DATA-003 (abgesagt)** No original-CSV import: the game is 100% self-contained — runtime reads only `res://data/*.json` + seed fallback; every CSV hit in `src/` is a comment, not a read. The curated `data/characters.json` (10 chars) + seed is the sole source. `Tools/EraDataImporter` stays `.csproj`-only.
- **WORLD-003c (boot-world composition, authored scenes)** The boot world is an **authored scene** (`scenes/RanchWorld.tscn`) — scene instances, not runtime `Instantiate()`: the 3D greybox instance + the **existing** `Game.tscn` (2D management UI) instance on a CanvasLayer, both on the single shared `GameRoot` autoload — no second economy/clock/job path. `RanchWorldController` only switches the active presentation and routes input through the greybox's single `WorldInputGate` (open/closing management suspends/resumes world movement + camera + interaction safely; Esc returns to the world; in-world "Open Management UI" button is an authored TSCN `[connection]`). Also fixed two authored-scene defects: `CameraRig.Target` was never authored (camera never followed the player) — now `Player/CameraTarget` NodePath in the TSCN; the management button was unwired. `MainMenuController.GoToGameScene` routes to the composed boot world (exported `GameScenePath`). `TestRanchWorldComposition`: 23 dedicated smoke assertions. Full isolated smoke **1176 assertions PASS** on Godot 4.7.2 mono.
- **WORLD-003d (first complete playable day, end-to-end)** The greybox is a live view for the **whole day**: `RanchGreyboxController` subscribes to `GameRoot.StateChanged` and re-derives daylight + roster automatically on every phase advance, assignment, mentorship, settlement, save, load, and new game — no manual refresh from any caller. `TestFullPlayableDay` proves one complete day on a single `GameRoot`: management assign → social mentorship → 3D live view → lighting across Morning/Afternoon/Evening/Night → settlement report → save/load round-trip (assignment, bond, morale, calendar intact; live view re-derives from the loaded state) → stale-generation rejection. 25 dedicated smoke assertions; also hardened `OnSharedStateChange` against a node freed without `_ExitTree` (self-detaches; closes an ObjectDisposedException regression). Full isolated smoke **1201 assertions PASS** on Godot 4.7.2 mono.
- **AI-001 (stand-in navigation + bounded stuck recovery)** `StandInNavigationMath` (pure, Node-free, headless-testable: `StepToward`, `IsArrived`, `IsStuck` = no-progress detection, `Recover` = snap to anchor, deterministic `Tick`) + opt-in walk layer in `RosterRig` (`WalkSpeed` 0 = original snap behavior the suite asserts; > 0 = stand-ins *walk* to their logical anchor each frame, bounded by `ArrivalTolerance` + `MinProgress` with bounded stuck recovery; `GetAvatar`/`GetTarget` for clean testing). Presentation only — reads the shared assignment, computes no work, never leaves the greybox bounds. `TestStandInNavigation`: 15 assertions (pure-math contract + one integration test driving the RosterRig in walk mode to prove a stand-in reaches its anchor within a bounded frame budget, in-bounds). Full isolated smoke **1216 assertions PASS** on Godot 4.7.2 mono.
- **UI-001 (management playthrough + narrow-viewport coverage)** Full management playthrough via the **real** UI shell (sidebar nav buttons + End Day button) and narrow-viewport compact-nav coverage. Fixed a real reachability defect: `hiddenScreens` hid 7 screens from **both** nav bars, but 3 of them (Save/Load, Settings, Milk) had no contextual entry point — `settings` was unreachable from anywhere in the game; now only screens with a guaranteed contextual path (report/visit/training/mental) stay hidden, while gated-but-nav-reachable screens (combat/research/milk) show as locked and are still reachable in the compact bar. Added a read-only `CurrentScreen` accessor. `TestManagementPlaythroughAndCompactNav`: 15 dedicated smoke assertions (sidebar nav navigates to the right screen, End Day button → report screen, compact bar visible + 13 of 13 management shortcuts reachable, no duplicate nav for non-nav screens). Full isolated smoke **1231 assertions PASS** on Godot 4.7.2 mono.
- **NSFW-GATE-001 (milk fail-closed gate + scope doc)** Closed a fail-open gap: `MilkEconomyService.ProduceMilk`/`ShipMilk` mutated milk stock/quality with **no** `AdultEligibilityGate` check (a minor-coded character with the constitution could produce/ship milk); both now deny non-`ConfirmedAdult` characters. Added 3 smoke assertions (minor blocked, unknown blocked, confirmed allowed). Authored `docs/NSFW_SCOPE.md`: separates what this agent can do (code, gates, non-explicit tooling) from what is blocked by content policy (explicit character models/text) and lists the gate-blocked roster. Full isolated smoke **1234 assertions PASS**.
- **NSFW-GATE-002 (training-dispatch fail-closed negative tests) — DONE.** Closed the last **untested fail-closed boundary**: `EnhancedTrainingService.PerformAction` is the main gameplay adult entry point, but every existing training test granted `ConfirmedAdult` to exercise other mechanics (energy/bond/tool/consent/cap) — **no negative test** proved it actually denies `Minor`/`Unknown`/`Ambiguous`. Added 26 assertions to `TestAdultEligibilityGate` proving the dispatch boundary is fail-closed: for each blocked state, `PerformAction` denies, names the eligibility block, resolves no action, produces no mental-state effects, gains no pleasure/pain, and mutates no mature state (character `PleasureA/B`/`FallState`, `TrainingHistory`, `TotalTrainingSessions`, `TotalMilkProduced` all unchanged). Plus a positive control that `ConfirmedAdult` is not denied *by the eligibility gate* (may still be blocked by other rules). This satisfies the Extra-criterion-1 bar: *"Gate proven fail-closed by negative tests at every boundary."* Build **0/0**, full smoke **1341 assertions PASS**. Note: `characters.json` has **zero** `ConfirmedAdult` characters (all `ApparentAge`/`AdultEligibility` null) — AC #8/#12 (adult NPCs / master hero) remain **gate-blocked on independent human design review** (a hard stop condition this agent does not self-grant).
- **WORLD-INPUT (controller support) — DONE (buttons verified headless; stick sign live-only).** `3D_REMAKE_PLAN` requires "keyboard plus controller mappings" and `WorldInputBootstrap`'s docstring promised "keyboard + gamepad", but **only keyboard events were ever registered**. Added: (a) discrete **gamepad button** InputMap bindings (A/Start = interact, B = recenter, L1/R1 = zoom) using the correct GodotSharp 4.7.0 API (`JoyButton` — `JoypadButton`/`JoypadAxis` do NOT exist in this binding; a stale-assumption trap the compiler caught); (b) **analog stick** movement + camera look read directly via `Input.GetJoyAxis(0, JoyAxis.LeftY/LeftX/RightY/RightX)` in `ThirdPersonPlayerController` (left stick) and `WorldCameraRig` (right stick) — the canonical Godot pattern for directional sticks (a single `JoypadMotion` InputMap event can't encode up-vs-down); (c) movement blends keyboard + stick (larger-magnitude axis wins) so keyboard still works with no gamepad attached. Removed the now-dead `camera_look_*` actions + `LookSensitivity` (look is stick-driven). 13 new headless smoke assertions verify the action set + button bindings. Build **0/0**, Smoke **1354 OK**.
  - **HONEST CAVEAT (live-only):** the analog stick **direction sign** (`-LeftY` = forward, `-RightY` = look up) follows the standard Godot convention but **cannot be verified headlessly** (no physical joypad in CI; `Input.PushEvent`/`ParseInputEvent` are absent from this binding) — confirm with a real controller before marking controller support fully verified. The **button** mapping IS headless-verified.
- **WORLD-CAMERA (mouse/keyboard camera control) — DONE (look signs verified; feel is live-only).** Discovered camera look was **dead code**: the `camera_look_*` actions were registered but **never bound** to any input (no keys, no buttons, no mouse), so `IsActionJustPressed` could never fire — a keyboard/mouse user (the default, given the cursor UI) had **no camera rotation**, and zoom had **no keyboard path** (only gamepad L1/R1). That broke AC #6 for the primary input device. Fixed: (a) **RMB-hold mouse-look** via event-driven `_UnhandledInput` on `InputEventMouseMotion.Relative` (coexists with the cursor UI — the cursor is never hidden; the world only looks while RMB is down); (b) **Q/E keyboard zoom** bound to the existing `camera_zoom_in`/`camera_zoom_out` actions (this GodotSharp 4.7.0 binding has **no** `InputEventMouseWheel`, so wheel zoom is not wired — keys + gamepad shoulders cover it); (c) new `WorldCameraMath.ApplyLook` (yaw/pitch deltas, pitch clamped) + `WorldCameraRig.ApplyLookDelta`, with the **look signs verified** against the camera-position/look-at math (mouse-right → view-right, mouse-up → view-up). Removed the Godot-3 `InputEvent.Accept()` call (not in 4.x). 3 new headless smoke assertions cover `ApplyLook` (yaw add, pitch clamps min/max). Build **0/0**, Smoke **1357 OK**. **Caveat:** mouse-look *feel* (sensitivity, inversion) is a live-hardware check.
- **ART-001a (first real 3D asset — Blender-built barn)** Proved the **Blender → GLB → Godot → smoke** pipeline. `Tools/Blender/build_barn.py` builds a low-poly gable barn (body, roof, door, trim, silo) headlessly in Blender 5.2.1 (Steam) and exports `assets/3d/barn.glb`. `Tools/Blender/render_barn.py` renders a Cycles preview to verify geometry headlessly (vision-checked). `RanchGreybox.tscn` instantiates the barn at the milk station (replaces the pure-box world there). Smoke test asserts the barn instantiates and exposes meshes (regression anchor for the pipeline). **Non-adult world asset**; adult character models remain gate-blocked per `ADULT_CHARACTER_VALIDATION.md`. Verified: Blender export OK, Godot import OK, SMOKE PASS.
- **ART-001b (non-adult 3D scenery)** Fence + tree + well built headlessly in Blender 5.2.1 (Steam) and shipped as GLB: `Tools/Blender/build_world_assets.py` (low-poly fence 2 posts/2 rails/caps; tree trunk + 3 canopy cones; well stone ring + posts + gable roof with fixed normals) + `render_world_assets.py` (Cycles preview of the built GLBs, vision-checked). `RanchGreybox.tscn` gains a `Scenery` node (well, 3 trees, 2 fence runs) within the 40×30 greybox bounds. 6 smoke assertions anchor the pipeline (scenery present, well/trees/fence instantiate, well in-bounds). **Non-adult world assets only** — adult character models stay gate-blocked. 0 warn / 0 errors, SMOKE PASS.
- **ART-001c (non-adult 3D props)** Hay bales, water trough, broadleaf trees, path stones via the same proven pipeline: `Tools/Blender/build_props.py` exports 4 GLBs; `render_props.py` previews the built GLBs; `verify_props.py` confirms per-GLB object counts (the deliverable, not a preview angle). `RanchGreybox.tscn` `Scenery` node adds Hay_A/Hay_B (barn corner), Trough (pasture), Broadleaf_A/B (edge), PathStones (player→station path) — all in the 40×30 bounds. 5 new smoke assertions (hay, trough, 2× broadleaf, stones instantiate + in-bounds). **Non-adult world assets only**. 0 warn / 0 errors, SMOKE PASS.
- **ART-001d (non-adult 3D props, batch 2)** Signpost, crates+woodpile, grass/flower tufts, low hedge via the same pipeline: `Tools/Blender/build_props_d.py` (per-asset material recreation after `clear_scene`, since `read_factory_settings` removes data blocks) exports 4 GLBs; `verify_props_d.py` confirms mesh counts (signpost 4, crates 8 = 2 crates + 6 logs, grass 29, hedge 2). `Scenery` adds Signpost (path), Crates (barn corner), Grass_A/B (patches), Hedge (back edge) — all in bounds. 5 new smoke assertions. **Non-adult world assets only**. 0 warn / 0 errors, SMOKE PASS.
- **ART-001e (PBR ground + pasture enclosure)** Material Maker 1.7 headless pipeline established (Godot `--export-material` on the material-maker source project; `mm-mcp` server configured). Grass-field node graph (t04_grass_field) rendered to albedo/normal/ORM + authored `grass_field.tres` (StandardMaterial3D, `uv1_scale 8,6`) replaces the flat procedural ground in `RanchGreybox.tscn`; `pasture_boundary.glb` 3-sided fence loop encloses the pasture. 7 new smoke assertions (PBR material + albedo/normal/ORM valid, fence node present). Full isolated smoke **1261 → 1267 assertions PASS**.
- **ART-001f (PBR wood baked into fence/signpost/crates/pasture GLBs)** Painted-wood-siding graph rendered (1024, downscaled from 2048). Discovered `material_override` is a **no-op on GLB `Node3D` roots** (property belongs to `GeometryInstance3D`, silently dropped) — fixed by baking the PBR material into each GLB's material slots via Blender headless (`Tools/Blender/bake_one.py`, one process per GLB to avoid memory crashes). Smoke verifies the baked material via the glTF JSON chunk (this GodotSharp build lacks `ArrayMesh.SurfaceCount`/`SurfaceGetMaterial`). **Critical: Godot's glTF importer externalizes embedded GLB textures to per-GLB PNGs and references them — the 12 external wood maps are mandatory and committed (deleting them broke all fence/signpost/crate/pasture loads).** Full isolated smoke **1270 assertions PASS**.
- **ART-001g (selective PBR bakes — bark, cobblestone, well wood)** `Tools/Blender/bake_select.py` bakes the right material onto the right meshes only: `tree.glb` + `tree_broadleaf.glb` bark on trunks (canopies untouched), `well.glb` wood on posts/rail/ring/roof (water untouched), `path_stones.glb` cobblestone on all stones. 24 more external maps committed. Full isolated smoke **1270 assertions PASS**.
- **SOFT-ANIME-001 (soft, natural anime shading system)** Per user direction — **no hard toon/cel band, no hard corners** — a soft, natural anime lighting model instead of a cel shader. `src/Character/SoftShadingMath.cs` (pure, Node-free, headless-verifiable reference model: soft diffuse ramp, hemisphere ambient, gentle fresnel rim; `Shade()` composites `albedo*(soft_diffuse*light+hemi)+rim`, clamped [0,1]) + `SoftShaderSource.cs` (Godot 4 spatial GLSL) + `SoftMaterialFactory.cs` (maps C# `SoftParameters` onto `ShaderMaterial`, Node-free). `CharacterAvatar3D.UseSoftShading` (opt-in, default off) routes body/head to the soft `ShaderMaterial`; the CHAR-001 debug stand-in stays `StandardMaterial3D` by default (existing assertion unaffected).
  - **GPU-verified (the headless suite could NOT catch this — see BUG-001 below):** the first shader hand-rolled `dot(NORMAL, -LIGHT)` in `fragment()`, which **fails Godot 4 GPU compilation** (`LIGHT` is only valid inside a `light()` function). Real GPU render via `src/Dev/WorldCapture.cs` exposed `SHADER ERROR: Unknown identifier 'LIGHT'` → avatars fell back to **flat unlit gray**. Corrected to the idiomatic path: `ALBEDO = base_color` (engine applies soft diffuse + hemisphere ambient via WorldEnvironment) + `SPECULAR=0` (soft matte) + `RIM=rim_strength` (subtle edge glow). A/B render (`src/Dev/ShadingAB.cs` + `ShadingAB.tscn`, Standard vs soft, one directional light) **objectively measured + vision-confirmed**: Standard = strong lit→shadow gradient; soft (rim 0.12) = **properly lit, soft low-contrast gradient, no cel band, no hard outline, no hard corners** — a credible soft-anime match. Rim default lowered 0.25→**0.12** (0.25 washed the surface flat against built-in diffuse). 18 soft-shader smoke assertions (incl. "no invalid LIGHT reference"). Build 0/0, full smoke **1311 PASS**.
  - **AC #13 (soft shading, visually inspected) + AC #22 (project actually launched + visually inspected): DONE.** Real GPU launch of the composed `RanchGreybox.tscn` (barn, fences, trees, well, ground, walls, sun, world environment) via `WorldCapture.tscn` → saved PNG → vision-inspected. Roster=2 avatars placed + soft-shaded.
- **BUG-001 (soft-shader failed GPU compile → flat unlit avatars)** **SHIPPED + GPU-verified fix.** The hand-rolled `dot(NORMAL, -LIGHT)` in `fragment()` is invalid in Godot 4 spatial shaders (`LIGHT` is only a built-in inside a `light()` function); the shader failed to compile and avatars rendered **flat unlit gray**. Root cause caught by the real-GPU `WorldCapture` render (headless smoke only checked the C# math + source string, so it passed). Fixed to the idiomatic `ALBEDO` + `SPECULAR=0` + `RIM` path (see SOFT-ANIME-001). Added a smoke guard: shader source must not contain a bare `-LIGHT` reference. **Lesson: any runtime shader must be GPU-render-verified, not just source-checked.**
- **WORLD-004 (NPC 3D interaction — AC #10 + AC #7 player-driven)** `CharacterAvatar3D` now implements `IWorldInteractable` exactly like `WorldStation`: `CharacterId`/`DisplayName`/`Dispatcher`/`InteractionGuard` + `Activate(context)` dispatches `WorldCommandKind.Mentorship` through the **shared `IWorldCommandDispatcher`** → `GameRoot.TryConductMentorship` (the same social/bond path as the station — no second simulation, no reward/economy computed by the avatar). `RosterRig` stamps `CharacterId`/`DisplayName`/`Dispatcher` + `WorldInteractionGuard` onto every placed avatar and exposes `AvatarIds` + `GetAvatar(id)`. `RanchGreyboxController.HandleInteract` now resolves the **nearest interactable** (station or avatar, both within `InteractionDistance`), so the player walks up to an NPC and presses `F` to mentor them — physical + management both call the same simulation. 14 new smoke assertions (avatar is an `IWorldInteractable`, guard blocks double-activation, stub dispatcher receives a `Mentorship` command with the avatar's id, nearest-target selection, dispatcher-bound + unbound cases). Build 0/0, full smoke **1311 PASS**. **AC #10 DONE** (player can interact with at least one NPC through the 3D world). **AC #11 DONE** (a real social/bond event is reachable from the 3D experience via the shared `TryConductMentorship`).
- **PERF-001 (GPU baseline) — DONE + measured.** Real-GPU baseline of the composed `RanchGreybox.tscn` (14 GLB PBR props + 2 soft-shaded avatars) via `src/Dev/PerfCapture.cs` using the compiler-verified `Performance.GetMonitor(Performance.Monitor.*)` API (the only stats API exposed to C# in 4.7.2 — `RenderingServer.Get*Count` are not bound). Measured: **4,143 draw calls**, **393,403 primitives**, **5,553 objects**, **2.44 GB video memory**, 2,833 nodes, 26,613 scene objects, 621 MB static memory, ~320 FPS engine cap. The 393k tris + 2.44 GB video memory come from the GLB PBR props — the main lever as the world scales. **No LOD/instancing needed yet** (scene runs at engine cap); decision gate: re-measure before large prop arrays / high-poly characters, trip at > ~8k draw calls / > ~800k tris / > ~4 GB video mem in a foreground window. Caveat: the isolated capture window is not foreground, so Windows throttles the render loop — the frame-delta number (72 ms) is OS-throttled, not a real in-game FPS; the stable signals are the GPU-cost metrics. Full detail + reproduce command in `docs/PERF_BASELINE.md`.

- **DREAM-LOOP (capture + judge pipeline) — DONE (tooling); scene 4.5/10 (Tier 2 ceiling).** `src/Dev/WorldCapture.cs` rewritten: golden-hour ProceduralSky + Environment (SSAO, low cool fog, filmic tonemap, key:ambient ~2:1) + a cool rim/fill DirectionalLight, two deterministic captures (wide establishing + close avatar) to `AppData/Local/OpenMakaiRanchCapture/`. 3 judge rounds (vision_analyze, gated 0-10 ladder): 4.0 → 4.5 → 4.5. Real wins locked in: cast shadows now render, grass less muddy, cream haze killed, sky readable. **Hard ceiling = asset fidelity, not lighting:** the ranch is greybox box primitives, so SSAO/rim/material-read (the Tier-3 gates) have nothing to bite — judge correctly pins 4.5. Per the dream-loop skill's anti-loop rule (2 stalls = rethink, don't grind), I stopped tuning lighting and recorded the real next lever. Reproduce: build, then run `WorldCapture.tscn` in an isolated profile (see `.dream-loop/CONCEPT.md`).

- **WORLD-TOWN-001 (town area + shop counter → existing economy) — DONE.** First original area beyond the ranch made physically present: `scenes/Town.tscn` (3D area with a working **shop counter** `WorldStation`, ground + sun + world environment, town props reusing existing GLBs — no new assets). Wired through the canonical `WorldCommand` boundary, so the world purchase uses the **same `ShopService`** the management UI uses — **no second economy**. Implementation: new `WorldCommandKind.ShopBuy` (`IWorldCommandDispatcher.cs`) → `GameRootCommandDispatcher` routes `ShopBuy` → `GameRoot.TryBuyItem(itemId, qty, generation)` (StateGeneration-guarded, calls the single `Shop.Buy` = `Economy.Spend` + `Inventory.AddItem`). `TestTownShopCounter`: the counter buys through GameRoot, spends the exact item price, grants the item to the shared inventory, and **denies with no state mutation** when broke + rejects a **stale generation** (fail-closed boundary, mirroring WORLD-002). `TestTownSceneIsLive`: the authored town scene instantiates into a real area with a working `ShopBuy` counter. 16 new smoke assertions. Build 0/0, full smoke **1373 PASS**.

- **WORLD-TOWN-002 (town guild/recruitment counter → existing roster) — DONE.** The town is now a real **multi-service area**, not a single-shop stub (the original town has general store + guild + research, per `TownService.Actions`). Second POI `GuildCounter` in `scenes/Town.tscn` wired to the **existing `RecruitmentService.HireOffer`** (spend `DefaultRecruitCost` → add character to the shared roster — the same roster the UI reads). Implementation: new `WorldCommandKind.Recruit` → `GameRootCommandDispatcher` → `GameRoot.TryRecruit(generation)` (StateGeneration-guarded, calls the single `Recruitment.HireOffer`). `TestTownGuildCounter` (7 assertions): the guild counter hires through GameRoot, adds a character to the same roster, spends the recruit cost, **denies with no state mutation** when broke, and rejects a **stale generation** (fail-closed). Build 0/0, full smoke **PASS** (1380 OK). No second roster/economy path.

- **WORLD-TOWN-003 (walkable traversal ranch ↔ town) — DONE.** The player can now travel between the ranch and town via **travel-gate smart objects** (`WorldTravelGate : Area3D, IWorldInteractable`), a new `ITravelHandler` presentation boundary. Design: travel is **presentation** — no gold, no clock, no job, no simulation number moves. The composition (`RanchWorldController`) implements `ITravelHandler.TravelTo(areaId)`: swaps `Visible` + `ProcessMode` across all areas, repositions the active area's player to its `EntryPosition`, re-derives daylight + roster. Each area is now a self-contained `RanchGreyboxController` root with its own player/camera (the town scene gained both — it is genuinely walkable, not just an inert counter set). `RanchWorld.tscn` now instantiates **both** areas. `RanchGreyboxController` generalised to multi-station (`CollectStations`) so the town's two counters are discovered + bound. `WorldTravelGate` reuses the same `WorldInteractionGuard` + `IWorldInteractable` contract as `WorldStation` but routes to `ITravelHandler` (presentation) instead of `IWorldCommandDispatcher` (simulation) — a deliberate separation: activating a gate must never move a simulation number. `TestTravelRoundTrip` (35 assertions): boot in ranch → gate activates → town visible / ranch hidden / town player at entry / generation unchanged → return gate → ranch visible / town hidden / ranch player back at entry / no-op on same-area → **save/load round-trip (AC #16)**: travel to town → `PlayerState.CurrentArea` = "town" → save → load → fresh world boots in the town with town visible / ranch hidden / player at entry. Build 0/0, full smoke **1415 PASS** (was 1380). No second simulation.
  - **Follow-ups (READY):** WORLD-TOWN-004 = town **environment art pass** (greybox → themed plaza) once asset-fidelity budget allows. WORLD-TOWN-005 = **adventure area** as a third authored area (original `冒険` zone). Design + area inventory + all world cards live in `docs/WORLD_DESIGN.md`.

- **WORLD-ADVENTURE-001 (adventure area + patrol gate → existing AdventureService) — DONE.**
  The original's third major area (`冒険`) is now a real 3D area: `scenes/Adventure.tscn`
  (`AreaId="adventure"`, player + camera, ground + sun + world environment, adventure props
  reusing existing GLBs). Its **patrol gate** (`WorldStation`, `CommandKind=5` `RunMission`,
  `CommandTargetId="field_clear"`) routes through the canonical `WorldCommand` boundary →
  `GameRootCommandDispatcher` → **`GameRoot.TryRunMission`** (generation-guarded, calls the
  single existing `RunMission` → `AdventureService.ResolveMission`). **No second combat/economy
  path.** `RanchWorldController` now discovers areas by child (any `RanchGreyboxController`
  child auto-registers) — the ranch stays primary; town + adventure register by their `AreaId`.
  Ranch has two travel gates (town + adventure); the adventure has a travel gate back to the
  ranch. `TestAdventureSceneIsLive` (16 assertions): scene loads, area id correct, patrol gate
  present + bound + dispatches `RunMission` with a mission id, travel gate back to ranch,
  mission runs through GameRoot (shared `AdventureState` updated — not a second sim), stale
  generation rejected. `TestTravelRoundTrip` updated: ranch has two travel gates, adventure
  area present. Build 0/0, full smoke **1431 PASS** (was 1415). No second simulation.
  - **Follow-up (READY):** WORLD-ADVENTURE-002 = adventure **environment art pass** (greybox →
    themed wilderness) once asset-fidelity budget allows.

- **BARN-001 (barn PBR material — verified; art ceiling is geometry) — DONE (material).** The
  barn was the single most visible texture-less element (15 KB, 0 textures while 8 sibling props
  carry baked PBR). Reused the **already-verified** `w03_painted_wood_siding` PBR set (albedo +
  normal + ORM) — no new generation — and baked it into the barn's 4 wood meshes (Barn_Body,
  Barn_Door, Barn_DoorFrame, Barn_LoftWindow); a dark solid for the roof and a galvanized-metal
  solid for the silo. Blender 5.2.1 headless export → self-contained **15 KB → 1.8 MB** GLB with
  embedded PBR (verified via Blender re-import: 3 textures present on every wood mesh, 190 tris,
  7 meshes). Godot `--import` clean (104 steps, 0 errors); the scene instances `barn.glb`
  directly so the PBR flows through with no scene change. **Honest judge result:** a fresh
  golden-hour wide capture (vision_analyze) scores **~4/10** — the barn now *does* read as a
  wood structure (a real improvement over the flat grey box), but the texture reads washed-out
  on a 12-tri box and the **dominant weakness is the hard boxy low-poly geometry** (90° cubes),
  which directly contradicts the "no hard corners" target and **no texture pass can fix**. This
  confirms the dream-loop ceiling is **asset-geometry fidelity, not material** — the next real
  lever is re-modeling the hero props (barn/roof/crates) with rounded/beveled silhouettes, a
  much larger effort, deferred behind P0/P1 systems. Non-adult asset.

## Next — priority order

Engine: use **Godot 4.7.2 mono** from `E:\GodotEditor\Godot_v4.7.2-stable_mono_win64.exe`. `launch.py` auto-discovers it (rejects the 198 KB `*_console.exe` stub by size, prefers the highest 4.7.x). No `GODOT_BIN` needed.

Blender (Steam): `D:\SteamLibrary\steamapps\common\Blender\blender.exe` (5.2.1 LTS) — headless `--background --python` build + `gltf` GLB export proven (ART-001a). Blender MCP not configured; direct script calls are the working path. ComfyUI: `D:\ComfyUI` (not running by default; start via `start-comfyui.bat` if image tooling is needed).

1. **CHAR-002 / ART-002 (gate-blocked)** One real character (candidate: Noir) — master source/context review, non-explicit identity references, .blend/GLB + material/rig/export validation, shared Godot **soft-anime shading prototype** (SOFT-ANIME-001 shipped — the soft, natural look, no hard toon band). Gate-safe: only `ConfirmedAdult` after **independent design review**; no age renumbering. Blocked: `data/characters.json` has no `AdultEligibility` field, no ConfirmedAdult character, no design review yet. Needs CHAR-001 (done) + independent design clearance + asset generation.
2. **MORPH-001 / ANIM-001** Gameplay-to-visual curves + shared rig/animation, once CHAR-002 is unblocked.

## Ready after prerequisites

- **CHAR-002** One master character source/context review and non-explicit identity references. Candidate only: Noir; no age/design clearance yet. Needs CHAR-001 (done) + independent design review (ConfirmedAdult) + ComfyUI asset generation.
- **ART-002** Master character .blend, GLB, material/rig/export validation; shared Godot toon prototype. Needs selected references; no mass conversion.
- **MORPH-001** Gameplay-to-visual curves, matching body/clothing shapes, extreme-combination lab. No mesh internals in saves.
- **ANIM-001** Shared rig and idle/walk/run/work/talk animation validation at gameplay camera.
- **AI-001 (done)** Stand-in navigation + bounded stuck recovery shipped (`StandInNavigationMath` + opt-in `RosterRig` walk layer; placement core in WORLD-003a). 1216 smoke PASS.

## Backlog / isolated follow-ups

- **CORE-003** Night-training growth multiplication: exact reproduction and original-rule comparison.
- **CORE-004** Settlement idempotency, effective-job semantics and report ledger tests.
- **TOOLS-002** Raw MCP request limits/authentication/development-only export policy; do not equate adapter policy with endpoint security.
- **TOOLS-003** Isolate engine editor-shutdown warning; no engine upgrade mixed into game work.
- **UI-001 (done)** Management playthrough + narrow-viewport coverage shipped — see Done list.
- **BUG-001 (fixed)** CS8604 in UiShellController.Screens.cs:3598 (nullable Path.Combine path1) resolved with `?? Directory.GetCurrentDirectory()` fallback; clean `--no-incremental` build now 0 warnings / 0 errors.
- **DATA-004** Replace placeholder ContentValidator with actual bounded JSON/reference validation.
- **PARITY-001** Original formula/fixture comparison matrix; no blanket parity claims.

## Deferred

Weather, mass character conversion, expensive secondary motion, portrait rendering, broad UI redesign and explicit presentation are not blockers for the first non-explicit 3D loop. Engine maintenance upgrades require a separate verified baseline checkpoint and task. No 4.8 development upgrade.
